export type LabelSource = {
  alertHash?: string;
  alertId?: string;
  bot?: {
    id: string;
    image: string;
    imageHash: string;
    manifest: string;
  };
  chainId?: number;
  id?: string;
};
